#include "SizeTConverter.hh"
#include "SCharacter_info.hh"

int			SCharacter_info::Update_from_data_string(const std::string &data)
{
  size_t		numeric_values[4];
  size_t		idx{0};
  size_t		idx_value{0};
  SizeTConverter	converter;

  while (idx < data.size())
    {
      if (
      converter.atoi();
      ++idx;
    }
}
